# Docker-specific environment variables
# Use these when running the app inside Docker Compose so DATABASE_URL points to the Postgres service `db`.

DATABASE_URL="postgresql://postgres:postgres@db:5432/clinique"
JWT_SECRET="supersecret"
PORT=4000

# Reminder:
# - Use `.env` for local development (points to localhost).
# - Use `.env.docker` for Docker Compose (points to service name `db`).
